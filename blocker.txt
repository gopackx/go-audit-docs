Remaining items that need you (Andrian), not code:

1. Replace placeholder assets
   - public/go-audit-icon.png (512x512 dummy shield)
   - public/og-image.png     (1200x630 dummy card)
   Regenerate via: python scripts/gen-logo.py

2. Mobile responsive testing
   - Can't test visually from here. Run `bun run dev`, open browser
     devtools, check breakpoints sm/md/lg on /, /docs, and a docs
     content page.

3. Push to GitHub
   - git init && git add -A
   - git commit -m "feat: initial docs site"
   - gh repo create go-audit-docs --public --source=. --push
     (or add a remote manually).

4. Connect to Vercel
   - Import repo on Vercel (framework auto-detected as Next.js).
   - vercel.json pins Bun as installer; no env vars required.
   - Add custom domain: go-audit.andrianprasetya.com
   - DNS: CNAME record `go-audit` -> `cname.vercel-dns.com`

Stack locked in:
- Next.js 16 + React 19.2
- Fumadocs UI 16 + Fumadocs MDX 12
- Tailwind CSS 4 (CSS-first via app/global.css)
- Node 20+ required
- Bun 1.2+ as package manager
